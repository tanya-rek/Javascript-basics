// 5
let distanceValue = prompt(
  "Please write a distance in km between the two cities"
);
let timeValue = prompt("Please write the time you want to cover this distance");
const yourSpeed = distanceValue / timeValue;
alert(yourSpeed);
